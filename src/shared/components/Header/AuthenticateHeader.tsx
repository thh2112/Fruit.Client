import Logo from '@/shared/components/logo/Logo';
import { AuthenticateHeaderContainer } from '@/shared/styled-components/header';

export const AuthenticateHeader = () => {
  return (
    <AuthenticateHeaderContainer>
      <Logo />
    </AuthenticateHeaderContainer>
  );
};
